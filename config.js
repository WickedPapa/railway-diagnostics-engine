// Impostazioni di configurazione globale
const CONFIG = {
    // Titolo dell'applicazione (mostrato nell'header e nel tab del browser)
    APP_TITLE: "Railway Diagnostics Engine",

    // Imposta a true per attivare di default l'evidenziazione delle celle quando cambiano di valore
    DEFAULT_HIGHLIGHT_CHANGES: true,

    // Colonne di base obbligatorie da bloccare e mostrare sempre
    MANDATORY_COLUMNS: ['THING', 'TIMESTAMP', 'SOURCE', 'LONG_DESCRIPTION', 'TSTAMP']
};
